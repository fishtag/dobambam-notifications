#!/usr/bin/env sh

if [ "$RACK_ENV" == "production" ] || [ "$RACK_ENV" == "staging" ]; then
  bundle exec puma -t 0:5 -w ${PUMA_WORKERS:-4} -p $PORT -e $RACK_ENV
else
  bin/rails server -p $PORT -e $RACK_ENV
fi
